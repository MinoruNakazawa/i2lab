{
  "name": "reserve31_205",
  "version": "1.0.0",
  "private": true,
  "description": "会議室予約カレンダー (31-205 / 31-202) with backend API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check api/lambda.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.835.0",
    "express": "^4.21.2"
  }
}
